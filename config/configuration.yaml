# https://www.home-assistant.io/integrations/default_config/
default_config:

# https://www.home-assistant.io/integrations/logger/
logger:
  default: info
  logs:
    custom_components.anova_oven: debug

debugpy:
  start: true

http:
  use_x_forwarded_for: true
  trusted_proxies:
    - 172.30.33.3   # see EDIT below
    - 127.0.0.1
    - ::1
